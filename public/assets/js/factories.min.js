(function() {
    'use strict';

    angular.module('lunchFactories', []);

})();


(function() {
    'use strict';

    angular
        .module('lunchFactories')
        .factory('categories', categories);

    categories.$inject = ['$http'];

    function categories($http){
        return {
            getCategories: getCategories
        };

        function getCategories(){

            return $http.get(app_templates.factories.categories).then(function(response)
            {
                return response.data;
            });
        }
    }

})();


(function() {
    'use strict';

    angular
        .module('lunchFactories')
        .factory('dishes', dishes);

    dishes.$inject = ['$http'];

    function dishes($http){
        return {
            getDishes: getDishes,
            find: find
        };

        function getDishes(){

            return $http.get(app_templates.factories.dishes).then(function(response)
            {
                return response.data;
            });

        }
    }

})();


//# sourceMappingURL=factories.min.js.map
